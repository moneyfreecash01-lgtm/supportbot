services:
  - type: web
    name: supportbot
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run prod
    healthCheckPath: /
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGO_URI
        sync: false
